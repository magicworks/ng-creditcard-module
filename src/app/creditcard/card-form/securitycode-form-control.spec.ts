import { SecuritycodeFormControl } from './securitycode-form-control';

describe('SecuritycodeFormControl', () => {
  it('should create an instance', () => {
    expect(new SecuritycodeFormControl()).toBeTruthy();
  });
});
